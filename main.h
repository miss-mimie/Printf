#ifndef _MAIN_H_
#define _MAIN_H_

#include <stdio.h>
#include <unistd.h>
#include <stdarg.h>
#include "main.h"

int _putchar(char c);
int _puts(const char *str);
int _printf(const char *format, ...);

#endif
